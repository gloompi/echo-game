# Environment variables are optional. Node does not automatically load this file.
PORT=3000
HOST=0.0.0.0
# Set to the public origin when deploying behind a reverse proxy.
# ALLOWED_ORIGINS=https://echo.example.com
MAX_ROOMS=64
